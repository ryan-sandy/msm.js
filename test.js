'use strict';
/*jslint node:true, indent:2*/

var server = require('./server.js');

/*
server.list(function (err, servers) {
  console.log(servers);
});

server.listPlayers('leeway', function (err, players) {
  if (err) { return next(err); }
  console.log(players);
});

server.say('leeway', 'Hello; echo "Hello hacker"', function (err) {
  console.log(err);
});

server.whiteList.remove('leeway', 'thesandyman123', function (err) {
  console.log(err);
});

server.whiteList.off('leeway', function () {
});

server.whiteList.on('leeway', function () {
});

server.whiteList.add('leeway', 'thesandyman123', function () {});
*/
